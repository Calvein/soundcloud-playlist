for track in tracks
    li(data-id=track.id).track: figure
        img(src=track.getImage())
        figcaption
            button.track-play
            .track-details
                span= track.getUsername()
                b= track.get('title')
        a(href='#').track-delete ×
